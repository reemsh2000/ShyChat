const signup = require('./signup');
const login = require('./login');
const logout = require('./logout');
const editProfile = require('./editProfile');

module.exports = {
  login, logout, signup, editProfile,
};
